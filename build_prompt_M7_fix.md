Quick consistency fix before closing M7: `Uninstall()` in `cli/cli.go` still uses the noisy `c.runner.Run(ctx, "launchctl", launchagent.BootoutArgs(...)...)` for its own best-effort bootout, not the `RunQuiet` you just introduced for `Install()`. If someone runs `exo uninstall` when nothing is currently loaded (e.g., running it twice in a row), it'll print the same misleading `Boot-out failed` message we just fixed for install. Swap it to `RunQuiet` there too for consistency, add/update a test mirroring `TestInstallUsesQuietBootoutBeforeBootstrap` for this case, and run `go build ./...`, `go vet ./...`, `go test -race -count=1 ./...` and paste output.
