I reviewed M4 directly — the `singleton`, `lifecycle`, `launchagent`, and `cli` packages are solid, I verified `github.com/tprasadtp/go-launchd` is a real, legitimate dependency (checked its actual source, confirmed the `syscall.ESRCH` fallback path is correctly grounded), and `go build`/`go vet`/`go test -race -count=3` all pass cleanly.

One real gap: `backend/backend.go` — the package that actually wires together `singleton.Acquire`, `lifecycle.IdleMonitor`, the listener (real-or-fallback), and `termserver.New` into the `Run()` orchestration function — has **zero tests** (`?   	github.com/DiegoAvila-yeyo/exo/backend	[no test files]`). This is the most important piece of M4's wiring: it has real concurrency (a `select` over `ctx.Done()`, `shutdownCh`, and `serveErrCh`) and a `cleanupOnce`-guarded shutdown path, and none of it is verified automatically. A subtle bug here (e.g., cleanup not running on some exit path, the lease not actually being released after idle shutdown, a double-shutdown race) wouldn't be caught by any of the existing tests, since they only exercise the constituent pieces in isolation.

Add tests for `backend.Run()` using the fallback listener path (no real `launchd` needed, since `activatedOrFallbackListener` already falls back to a plain `net.Listen` when not running under `launchd` — that's exactly the path these tests should exercise):

1. `Run()` with a short configured `IdleTimeout` actually triggers the full idle-shutdown sequence end-to-end: the server accepts a connection/request, goes idle, and after the timeout + grace period, `Run()` returns (not hangs), and the lease file is actually released afterward — verify this by calling `singleton.Acquire` on the same lock path immediately after `Run()` returns and confirming it succeeds (proving the first instance released it).
2. A second call to `backend.Run()` (or a lower-level `singleton.Acquire` call) against the same lock path *while the first `Run()` is still active* fails with `singleton.ErrLeaseHeld` — proving the single-instance guarantee actually holds through the real `Run()` path, not just in isolated `singleton` package tests.
3. Cancelling the context passed to `Run()` causes a clean shutdown (lease released, no goroutine/resource leak you can reasonably detect, e.g. via checking the HTTP server actually stopped accepting).
4. If you find an actual bug while writing these tests (rather than just confirming everything already works), fix it and describe what was wrong.

Run `go build ./...`, `go vet ./...`, `go test -race ./...` (the whole module, not just `backend`), and paste real output.
