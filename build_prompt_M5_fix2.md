The fix for the flaky reconciliation test is confirmed solid — I independently ran `go test -race -count=1 -run TestRunReconcilesCrashedInstanceSessions ./backend/...` 10 times and `go test -race -count=5 ./...` for the full suite, both clean.

Yes, please add the direct unit test you offered: a test in `sessionstore` (not relying on the slower end-to-end `backend` test) that specifically covers "process group still alive, but the recorded leader PID is gone" — spawn a real process group in the test, kill just the leader process while leaving another process in the same group alive (e.g., spawn a parent that forks/backgrounds a child then have the parent exit, or use two separate `exec.Command` calls with `Setpgid` pointing at the same target group if that's easier to construct deterministically), seed the store with a stale record pointing at that group/leader-pid combination, run `reconcileOne`/`Reconcile`, and assert it correctly reaps the group (`stale_reaped`) via the `TerminateProcessGroup` fallback path rather than giving up with `stale_orphaned`.

Run `go build ./...`, `go vet ./...`, `go test -race -count=1 ./...` (not cached) and paste real output once done.
